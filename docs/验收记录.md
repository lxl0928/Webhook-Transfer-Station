# 本地验收记录

日期：2026-09-16。验收针对本仓库当前实现，不代表已经完成真实第三方服务联调。

## 已通过

| 检查 | 结果 |
| --- | --- |
| Ruff check / format | 通过 |
| PostgreSQL 16 实际数据库 pytest | 70 项通过；含三平台 @ 格式、多源鉴权、载荷格式、夜莺事件、LLM 后追加 @、名单快照、API、重试及 SKIP LOCKED 并发领取 |
| Alembic 迁移 | 初版 0001 升降级已通过；本次带既有 Gitee 规则的 0001 → 0002 升级通过，数据保留、默认值正确，alembic check 无差异 |
| 前端 ESLint | 通过，0 warning |
| vue-tsc + Vite build | 通过；UI 组件按需注册，最大 JS chunk 约 444 kB（gzip 约 143 kB） |
| npm audit（官方 npm registry） | 0 漏洞 |
| docker compose config --quiet | 通过 |
| nginx -t | 通过；使用本机已有 nginx:1.27-alpine 测试实际配置 |
| nginx + 本地 FastAPI + PostgreSQL 冒烟 | 页面、/docs、/openapi.json、/health/ready、管理员登录均成功；413 响应同样包含 Trace ID |
| 浏览器 | 登录、规则列表、新增抽屉、日志列表及详情、资料保存、健康检测已实际操作验证；无浏览器 error/warn |
| 响应式 | 窄屏导航与退出入口可用；1365px 宽屏设置页两列，无页面横向溢出 |

测试使用独立 PostgreSQL 容器和临时 schema，HTTP 平台适配器使用 MockTransport。浏览器验收只创建了本地虚构规则和 ignored 消息，未向真实群发送通知。

## 未完成的外部验证

- 完整 Docker Compose 镜像构建尝试被 Docker Hub 认证服务网络超时阻断（拉取 `python:3.12-slim` 时失败）。因此没有宣称项目默认的 Python / Node / 非 root nginx 镜像已经整套构建和启动成功。Dockerfile、Compose 配置、实际 nginx 代理与本地服务分别完成验证；网络恢复后按 README 运行 `docker compose up -d --build`。
- 未提供真实 Gitee 仓库、LLM API Key、模型和群机器人凭证，尚未进行第三方端到端联调。三种平台的消息体、签名和业务响应校验已通过模拟 HTTP 测试。

## 建议首次部署验收

1. 配置公网 HTTPS 域名、LLM 主机允许列表并启动 Compose。
2. 修改默认管理员密码，保存模型配置并运行 LLM 健康检测。
3. 新增规则，在 Gitee 填入 URL 和相同回调密钥，触发 Push / PR / Tag / 评论事件。
4. 对照源站事件、日志输入、模型结果、目标 payload 与群消息，确认业务格式符合团队要求。

## 本次扩展验收

- 通用 JSON 对象、数组、字符串、布尔、纯文本和表单均通过 API/数据库测试；非法格式、null、NaN、未支持媒体类型有明确拒绝。
- Gitee 回归通过；夜莺单条触发/恢复与数组/events 批量识别通过。
- 飞书 Open ID、企业微信/钉钉用户 ID 与手机号、三平台 @所有人均验证实际 payload，含飞书注入防护、名单去重与长度限制。
- 真实 PostgreSQL 的 worker 测试覆盖 LLM 开启/关闭后追加 @、规则编辑不改变原消息快照名单。
- 浏览器使用虚构本地规则验证夜莺信息和人员回显，保存两位 Open ID 后通过 API 读取确认持久化；切换企业微信后旧名单清空并显示手机号字段；启用 @所有人时人员输入禁用，无浏览器 error/warn。
- OpenAPI 的通用内容类型与 HTTP 200 响应已实际读取验证；未向真实群发送通知。
- 真实平台 @ 成功与权限仍需实际群成员 ID、机器人凭证进行联调；不将 mock 协议验证描述为平台实发验证。

## Skill 与智能助手验收（2026-09-16）

| 检查 | 结果 |
|---|---|
| pytest + 真实 PostgreSQL 16 | **83 passed**，含 13 项新增 Skill/聊天测试 |
| Ruff lint / format | 通过 |
| ESLint / vue-tsc / Vite 生产构建 | 通过 |
| Alembic | 升级 head、check 无差异、0003 回退至 0002 后重新升级均通过 |
| Python wheel | 构建成功，归档中确认包含全部 11 个内置 SKILL.md |
| Docker Compose 配置校验 | 通过，无需新增服务或端口 |
| 浏览器 | Skill 列表、自定义项保存、全局聊天打开/关闭、历史展示、修改草案、确认执行和资料页回显通过 |

新增测试覆盖：内置初始化、工具目录、Skill CRUD/导出、非法工具拒绝、内置删除保护、跨用户隔离、读工具 wire 格式、LLM 请求格式和 Trace ID、写操作确认、取消、对象过期/变化、权限撤销、重复及并发确认只执行一次、敏感表单不进入历史、错误表单回滚并可重试、聊天异常释放租约，以及通过工具创建/更新 Skill、修改/删除规则。

浏览器采用独立本地 PostgreSQL 与本地模拟模型 HTTP 服务，实际观察到确认后用户资料发生变更。无真实模型或群机器人凭证，未调用真实付费模型或发送群通知。真实供应商是否支持 tools/tool_calls 及对话效果仍需部署环境联调。此前 Docker Hub 网络导致整套默认镜像构建未验证的限制仍保留；本次新增 Python 包及配置校验不等同于整套 Docker 启动验收。

临时浏览器页面、测试服务与 PostgreSQL 容器在验收后清理。
